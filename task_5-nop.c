#include "monty.h"
/**
 * nop_opcodes- does nothing
 * @head: stack head
 * @count: line_number
 * Return: Nothing
 */
void nop_opcodes(stack_t **head, unsigned int count)
{
	UNUSED(count);
	UNUSED(head);
}
